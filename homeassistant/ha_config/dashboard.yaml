# Lucius project — status panel Lovelace card (PLAN.md §9).
#
# RETIRED (Aug 2026, SQLite migration — see helpers.yaml's own note). This
# card showed 3 of the 7 status-writing flows' HA helper entities
# (Oura Sync, GoDaddy Pull, Body Composition Import — the other 4 flows
# were never added here even before the migration). All 7 flows now write
# their status to a shared SQLite table instead of HA entities, so there's
# nothing left for a Lovelace card built this way to display.
#
# If you already added this to a dashboard, it's safe to remove — the
# entities it references no longer get updated by anything. Status
# visibility going forward: WardStock's own status.php (GoDaddy, reads
# what Status Heartbeat pushes — always reachable, not home-network-only
# like this card was) and/or SQLite Web, browsing /share/lucius_status.db
# directly.
#
# Left below, commented out, as a historical record only — not something
# to re-add.
#
# type: vertical-stack
# cards:
#   - type: entities
#     title: "Lucius — Oura Sync (every 4h, anchored 10am)"
#     entities:
#       - entity: input_datetime.lucius_oura_last_success
#         name: Last successful sync
#       - entity: input_datetime.lucius_oura_last_attempt
#         name: Last attempt
#       - entity: input_boolean.lucius_oura_last_attempt_ok
#         name: Last attempt OK
#       - entity: input_text.lucius_oura_last_error
#         name: Last error (if any)
#   - type: entities
#     title: "Lucius — GoDaddy Manual Data Pull (every 15min)"
#     entities:
#       - entity: input_datetime.lucius_godaddy_last_success
#         name: Last successful pull
#       - entity: input_datetime.lucius_godaddy_last_attempt
#         name: Last attempt
#       - entity: input_boolean.lucius_godaddy_last_attempt_ok
#         name: Last attempt OK
#       - entity: input_text.lucius_godaddy_last_error
#         name: Last error (if any)
#   - type: entities
#     title: "Lucius — Body Composition Import (daily, ~noon)"
#     entities:
#       - entity: input_datetime.lucius_bodycomp_last_success
#         name: Last successful run
#       - entity: input_datetime.lucius_bodycomp_last_attempt
#         name: Last attempt
#       - entity: input_boolean.lucius_bodycomp_last_attempt_ok
#         name: Last attempt OK
#       - entity: input_text.lucius_bodycomp_last_error
#         name: Last error (if any)
