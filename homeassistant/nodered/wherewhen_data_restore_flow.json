[
  {
    "id": "tab_wherewhen_restore",
    "type": "tab",
    "label": "wherewhen - Data Restore",
    "disabled": false,
    "info": "PLAN.md §20 — reads a backup produced by wherewhen_data_export_flow.json and rewrites InfluxDB from it. Manual trigger ONLY, deliberately — explicitly for the wipe/migrate scenario (a freshly bootstrapped InfluxDB, new org/bucket/token per INFLUXDB_V2_SETUP.md), not for restoring into an instance that still has the old data. Edit the trigger's payload (backup_dir) before clicking, same pattern as oura_backfill_flow.json's manual trigger.\n\nGoDaddy-side restore is NOT reinvented here — that stays import.php reading the existing disaster-recovery JSON archive (PLAN.md §4), which already works. This flow is strictly the InfluxDB-only half.\n\nTHE RISKIEST PART OF THIS FLOW, flagged per PLAN.md §20's own caution that this is 'the highest-stakes flow in the project': reconstructing InfluxDB line-protocol from the annotated CSV InfluxDB's query API produces (n_wdr_parse_csv_to_lines). Handles multiple table/header blocks in one CSV response (Flux groups different tag-combinations of the same measurement into separate annotated blocks) by re-detecting a new header row whenever one appears, rather than assuming exactly one header block per file — REASONED FROM DOCUMENTED FLUX CSV FORMAT, NOT CONFIRMED against a real multi-table export. Writes one field per line-protocol point rather than combining a series' multiple fields into one line — relies on InfluxDB's own documented behavior of merging same-measurement/same-tags/same-timestamp writes at the storage layer, not on this flow doing that merging itself.\n\nSimplification, not full robustness: restores each measurement file in ONE write POST, not chunked like oura_backfill_flow.json's 500-line batches — acceptable for a rare, human-supervised operation, but flag if a real run ever hits a body-size error on a very large measurement.\n\nVerify mode (§20's 'still open' item, resolved here): after restoring, runs a Flux count() per measurement and compares against the export manifest's row_count — a BALLPARK sanity check (the manifest's row_count includes CSV header/annotation lines, so exact equality isn't expected), not a byte-exact guarantee.\n\nUNTESTED — never run against a live InfluxDB instance, and this flow's core logic could not be verified against real Flux CSV output during development. Companion test flow (wherewhen_data_restore_test.json) checks the CSV parser against a hand-built fixture matching the documented format, but that is not the same as a real InfluxDB response — treat this flow with real caution on first use, per its own tab info."
  },
  {
    "id": "n_wdr_trigger",
    "type": "inject",
    "z": "tab_wherewhen_restore",
    "name": "Manual trigger — EDIT payload before clicking",
    "props": [ { "p": "payload" } ],
    "repeat": "",
    "crontab": "",
    "once": false,
    "onceDelay": 0.1,
    "topic": "",
    "payload": "{\"backup_dir\": \"/share/lucius_data_backup/EDIT_ME\"}",
    "payloadType": "json",
    "x": 220,
    "y": 60,
    "wires": [ [ "n_wdr_capture_backup_dir" ] ]
  },
  {
    "id": "n_wdr_capture_backup_dir",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Capture backup_dir",
    "func": "const backupDir = (msg.payload || {}).backup_dir;\nif (!backupDir || backupDir.indexOf('EDIT_ME') !== -1) {\n    node.error('backup_dir not set — edit the trigger node\\'s payload before clicking (same pattern as oura_backfill_flow.json).', msg);\n    return null;\n}\nmsg.backup_dir = backupDir;\nflow.set('wdr_backup_dir', backupDir);\nreturn msg;",
    "outputs": 1,
    "x": 460,
    "y": 60,
    "wires": [ [ "n_wdr_read_secrets_file" ] ]
  },
  {
    "id": "n_wdr_read_secrets_file",
    "type": "file in",
    "z": "tab_wherewhen_restore",
    "name": "Read /share/lucius_secrets.json",
    "filename": "/share/lucius_secrets.json",
    "filenameType": "str",
    "format": "utf8",
    "chunk": false,
    "sendError": false,
    "encoding": "none",
    "allProps": false,
    "x": 720,
    "y": 60,
    "wires": [ [ "n_wdr_load_secrets" ] ]
  },
  {
    "id": "n_wdr_load_secrets",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Load secrets + start job log",
    "func": "let secrets;\ntry {\n    secrets = JSON.parse(msg.payload);\n} catch (e) {\n    node.error('Could not parse secrets file: ' + e.message, msg);\n    return null;\n}\nmsg.secrets = secrets;\nmsg.job_started_at = new Date().toISOString();\nflow.set('wdr_secrets', secrets);\nflow.set('wdr_job_started_at', msg.job_started_at);\nconst nowNs = Date.now() * 1000000;\nmsg.url = secrets.influxdb_url + '/api/v2/write?org=' + secrets.influxdb_org + '&bucket=' + secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = 'sync_job_runs,job_name=wherewhen_restore,status=started value=1 ' + nowNs;\nreturn msg;",
    "outputs": 1,
    "x": 970,
    "y": 60,
    "wires": [ [ "n_wdr_write_start_http" ] ]
  },
  {
    "id": "n_wdr_write_start_http",
    "type": "http request",
    "z": "tab_wherewhen_restore",
    "name": "InfluxDB: write start point",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 220,
    "y": 120,
    "wires": [ [ "n_wdr_parse_manifest_prep" ] ]
  },
  {
    "id": "n_wdr_parse_manifest_prep",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Set manifest filename",
    "func": "return { payload: flow.get('wdr_backup_dir') + '/manifest.json', secrets: flow.get('wdr_secrets'), job_started_at: flow.get('wdr_job_started_at') };",
    "outputs": 1,
    "x": 470,
    "y": 120,
    "wires": [ [ "n_wdr_read_manifest" ] ]
  },
  {
    "id": "n_wdr_read_manifest",
    "type": "file in",
    "z": "tab_wherewhen_restore",
    "name": "Read manifest.json (msg.payload = filename)",
    "filename": "",
    "filenameType": "msg",
    "format": "utf8",
    "chunk": false,
    "sendError": true,
    "encoding": "none",
    "allProps": false,
    "x": 720,
    "y": 120,
    "wires": [ [ "n_wdr_parse_manifest" ] ]
  },
  {
    "id": "n_wdr_parse_manifest",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Parse manifest, list CSVs",
    "func": "let manifest = {};\ntry {\n    manifest = JSON.parse(msg.payload);\n} catch (e) {\n    node.warn('Could not parse manifest.json (' + e.message + ') — verify mode will be skipped, restore still proceeds from whatever CSVs are present.');\n}\nflow.set('wdr_manifest', manifest);\nmsg.payload = 'ls -1 ' + flow.get('wdr_backup_dir') + '/*.csv';\nreturn msg;",
    "outputs": 1,
    "x": 720,
    "y": 180,
    "wires": [ [ "n_wdr_list_csv_files" ] ]
  },
  {
    "id": "n_wdr_list_csv_files",
    "type": "exec",
    "z": "tab_wherewhen_restore",
    "command": "",
    "addpay": "payload",
    "append": "",
    "useSpawn": "false",
    "timer": "10",
    "winHide": false,
    "oldrc": false,
    "name": "List *.csv in backup_dir",
    "x": 970,
    "y": 180,
    "wires": [ [ "n_wdr_parse_file_list" ], [], [] ]
  },
  {
    "id": "n_wdr_parse_file_list",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Parse file list (empty = nothing to restore)",
    "func": "const files = (msg.payload || '').split('\\n').map(s => s.trim()).filter(Boolean);\nmsg.secrets = flow.get('wdr_secrets');\nmsg.job_started_at = flow.get('wdr_job_started_at');\nif (!files.length) {\n    node.warn('No CSV files found in ' + flow.get('wdr_backup_dir') + ' — nothing to restore.');\n    return [null, msg];\n}\nmsg.files = files;\nreturn [msg, null];",
    "outputs": 2,
    "x": 220,
    "y": 240,
    "wires": [ [ "n_wdr_split_files" ], [ "n_wdr_job_success" ] ]
  },
  {
    "id": "n_wdr_split_files",
    "type": "split",
    "z": "tab_wherewhen_restore",
    "name": "Split: one message per CSV file",
    "splt": "\\n",
    "spltType": "str",
    "arraySplt": 1,
    "arraySpltType": "len",
    "stream": false,
    "addname": "",
    "property": "files",
    "x": 480,
    "y": 240,
    "wires": [ [ "n_wdr_read_csv_file" ] ]
  },
  {
    "id": "n_wdr_read_csv_file",
    "type": "file in",
    "z": "tab_wherewhen_restore",
    "name": "Read this CSV file (msg.payload = filename)",
    "filename": "",
    "filenameType": "msg",
    "format": "utf8",
    "chunk": false,
    "sendError": true,
    "encoding": "none",
    "allProps": false,
    "x": 730,
    "y": 240,
    "wires": [ [ "n_wdr_parse_csv_to_lines" ] ]
  },
  {
    "id": "n_wdr_parse_csv_to_lines",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Parse annotated CSV -> InfluxDB line protocol",
    "func": "// THE RISKIEST LOGIC IN THIS FLOW — see this tab's own info note for\n// the full explanation. KNOWN LIMITATION, not yet handled: this splits\n// each CSV row on plain ',' — it does NOT handle RFC4180-style quoted\n// fields that contain a literal comma inside a value (e.g. a string\n// field value like 'a, b'). None of this project's actual field values\n// are expected to contain commas today, so this is a real but currently-\n// low-risk simplification — worth fixing with a proper CSV-quote-aware\n// split if that ever stops being true. Handles multiple table/header blocks in one\n// response (Flux groups different tag-combinations under one measurement\n// into separate annotated blocks, separated by a blank line) by\n// re-detecting a new header row whenever one appears. BUG FIX (Aug 2026,\n// Fulgrim Flux-engine pass): the original heuristic here re-detected a\n// header only when the row differed from the previous one (a helper\n// array-equality check, since removed) — which silently misparsed a\n// REPEATED identical header row as a data row instead. That happens routinely in real Flux\n// output: two consecutive table blocks with the same tag SET but\n// different tag VALUES produce byte-identical header rows. Fixed by\n// requiring all three reserved markers ('_measurement', '_field',\n// '_value') as literal cell values on the SAME row, dropping the\n// differs-from-previous check entirely — real data will essentially\n// never carry all three of those exact strings as its own\n// field/measurement/tag values, so this is strictly safer, not riskier,\n// than the one-marker version it replaces. In THIS flow specifically the\n// old bug was low-impact in practice — the resulting phantom row's _time\n// value is literally the string \"_time\", which fails the NaN-timestamp\n// check further down and gets silently dropped, and a same-header-text\n// block's real data rows still parse fine against the stale (but\n// identical) header object. Fixed anyway rather than relying on that\n// incidental self-correction, and because the new analysis-engine flow\n// (analysis_engine_flow.json) adapts this same parser into one that\n// outputs row objects instead of line-protocol, without that downstream\n// NaN filter — there, the same bug would have silently fed a garbage row\n// into real analysis math instead of being dropped.\nfunction esc(s) { return String(s).replace(/([ ,=])/g, '\\\\$1'); }\n\nfunction parseFluxCsvToLines(csvText) {\n    const RESERVED = new Set(['result', 'table', '_start', '_stop', '_time', '_value', '_field', '_measurement']);\n    const rawLines = csvText.split('\\n').map(l => l.replace(/\\r$/, ''));\n    let header = null;\n    const outLines = [];\n    let dataRowCount = 0;\n\n    rawLines.forEach(raw => {\n        if (raw === '') { return; } // blank separator between table blocks — next real row re-detects header\n        if (raw.startsWith('#')) return; // #datatype/#group/#default — not needed to reconstruct line protocol\n        const cols = raw.split(',');\n        if (cols.includes('_measurement') && cols.includes('_field') && cols.includes('_value')) {\n            header = cols;\n            return;\n        }\n        if (!header) return; // stray row before any header seen — skip rather than crash the whole restore\n        if (cols.length !== header.length) return; // malformed/short row — same reasoning\n\n        const row = {};\n        header.forEach((colName, i) => { row[colName] = cols[i]; });\n\n        const measurement = row['_measurement'];\n        const field = row['_field'];\n        const value = row['_value'];\n        const time = row['_time'];\n        if (!measurement || !field || value === undefined || value === '' || !time) return;\n\n        const tsMs = new Date(time).getTime();\n        if (!Number.isFinite(tsMs)) return;\n\n        const tagParts = [];\n        header.forEach(colName => {\n            if (!RESERVED.has(colName) && colName !== '' && row[colName] !== undefined && row[colName] !== '') {\n                tagParts.push(esc(colName) + '=' + esc(row[colName]));\n            }\n        });\n\n        const isNumeric = !isNaN(Number(value));\n        const fieldPart = esc(field) + '=' + (isNumeric ? Number(value) : ('\"' + String(value).replace(/\"/g, '') + '\"'));\n        const tagStr = tagParts.length ? ',' + tagParts.join(',') : '';\n\n        outLines.push(esc(measurement) + tagStr + ' ' + fieldPart + ' ' + (tsMs * 1000000));\n        dataRowCount++;\n    });\n\n    return { lines: outLines, dataRowCount };\n}\n\nconst parsed = parseFluxCsvToLines(msg.payload || '');\nmsg.measurement_name = (msg.filename || '').split('/').pop().replace(/\\.csv$/, '');\nmsg.restored_line_count = parsed.lines.length;\n\nconst secrets = flow.get('wdr_secrets');\nmsg.url = secrets.influxdb_url + '/api/v2/write?org=' + secrets.influxdb_org + '&bucket=' + secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = parsed.lines.join('\\n');\nreturn msg;",
    "outputs": 1,
    "x": 990,
    "y": 240,
    "wires": [ [ "n_wdr_write_influx_http" ] ]
  },
  {
    "id": "n_wdr_write_influx_http",
    "type": "http request",
    "z": "tab_wherewhen_restore",
    "name": "InfluxDB: write restored lines (one POST per file — see tab info)",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 220,
    "y": 300,
    "wires": [ [ "n_wdr_join_files" ] ]
  },
  {
    "id": "n_wdr_join_files",
    "type": "join",
    "z": "tab_wherewhen_restore",
    "name": "Join: wait for all file restores",
    "mode": "auto",
    "build": "array",
    "property": "restored_line_count",
    "propertyType": "msg",
    "key": "topic",
    "joiner": "\\n",
    "joinerType": "str",
    "accumulate": false,
    "timeout": "",
    "count": "",
    "reduceRight": false,
    "comment": "Paired with n_wdr_split_files via msg.parts (mode: auto).",
    "x": 470,
    "y": 300,
    "wires": [ [ "n_wdr_prep_verify" ] ]
  },
  {
    "id": "n_wdr_prep_verify",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Prep verify pass (list of measurements to count)",
    "func": "const manifest = flow.get('wdr_manifest') || {};\nconst measurements = Array.isArray(manifest.measurements) ? manifest.measurements : [];\nmsg.secrets = flow.get('wdr_secrets');\nmsg.job_started_at = flow.get('wdr_job_started_at');\nmsg.total_lines_restored = Array.isArray(msg.payload) ? msg.payload.reduce((a, b) => a + (b || 0), 0) : 0;\nflow.set('wdr_total_lines_restored', msg.total_lines_restored);\nif (!measurements.length) {\n    msg.verify_results = [];\n    return [null, msg];\n}\nmsg.measurements = measurements;\nreturn [msg, null];",
    "outputs": 2,
    "x": 730,
    "y": 300,
    "wires": [ [ "n_wdr_split_verify" ], [ "n_wdr_build_verify_report" ] ]
  },
  {
    "id": "n_wdr_split_verify",
    "type": "split",
    "z": "tab_wherewhen_restore",
    "name": "Split: one message per measurement to verify",
    "splt": "\\n",
    "spltType": "str",
    "arraySplt": 1,
    "arraySpltType": "len",
    "stream": false,
    "addname": "",
    "property": "measurements",
    "x": 220,
    "y": 360,
    "wires": [ [ "n_wdr_prep_count_query" ] ]
  },
  {
    "id": "n_wdr_prep_count_query",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Build count() Flux query for this measurement",
    "func": "const secrets = flow.get('wdr_secrets');\nconst measurement = msg.payload;\nmsg.measurement_name = measurement;\nconst flux = 'from(bucket: \"' + secrets.influxdb_bucket + '\") |> range(start: 0) |> filter(fn: (r) => r._measurement == \"' + measurement.replace(/\"/g, '\\\\\"') + '\") |> count()';\nmsg.url = secrets.influxdb_url + '/api/v2/query?org=' + encodeURIComponent(secrets.influxdb_org);\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + secrets.influxdb_token, 'Content-Type': 'application/vnd.flux', 'Accept': 'application/csv' };\nmsg.payload = flux;\nreturn msg;",
    "outputs": 1,
    "x": 480,
    "y": 360,
    "wires": [ [ "n_wdr_count_http" ] ]
  },
  {
    "id": "n_wdr_count_http",
    "type": "http request",
    "z": "tab_wherewhen_restore",
    "name": "InfluxDB: query point count",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 720,
    "y": 360,
    "wires": [ [ "n_wdr_parse_count" ] ]
  },
  {
    "id": "n_wdr_parse_count",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Extract count from CSV response",
    "func": "const text = msg.payload || '';\nconst lines = text.split('\\n').map(l => l.trim()).filter(Boolean);\nconst headerLine = lines.find(l => !l.startsWith('#'));\nlet count = null;\nif (headerLine) {\n    const cols = headerLine.split(',');\n    const valueIdx = cols.indexOf('_value');\n    const dataLine = lines[lines.indexOf(headerLine) + 1];\n    if (dataLine && valueIdx !== -1) count = parseInt(dataLine.split(',')[valueIdx], 10);\n}\nreturn { payload: { measurement: msg.measurement_name, post_restore_count: (Number.isFinite(count) ? count : null) } };",
    "outputs": 1,
    "x": 970,
    "y": 360,
    "wires": [ [ "n_wdr_join_verify" ] ]
  },
  {
    "id": "n_wdr_join_verify",
    "type": "join",
    "z": "tab_wherewhen_restore",
    "name": "Join: wait for all verify counts",
    "mode": "auto",
    "build": "array",
    "property": "payload",
    "propertyType": "msg",
    "key": "topic",
    "joiner": "\\n",
    "joinerType": "str",
    "accumulate": false,
    "timeout": "",
    "count": "",
    "reduceRight": false,
    "comment": "Paired with n_wdr_split_verify via msg.parts (mode: auto).",
    "x": 220,
    "y": 420,
    "wires": [ [ "n_wdr_build_verify_report" ] ]
  },
  {
    "id": "n_wdr_build_verify_report",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Build verify report (ballpark, not exact)",
    "func": "// BALLPARK sanity check, per this tab's own info note — manifest\n// row_count includes CSV header/annotation lines from the export side,\n// so exact equality against post-restore point counts isn't expected.\n// This flags LARGE discrepancies (a wrong order of magnitude, or zero\n// when something was expected), not small ones.\nconst manifest = flow.get('wdr_manifest') || {};\nconst results = Array.isArray(msg.payload) ? msg.payload : [];\nconst manifestByName = {};\n(manifest.measurements || []).forEach(m => { manifestByName[m] = true; });\n\nconst warnings = [];\nresults.forEach(r => {\n    if (r.post_restore_count === null) {\n        warnings.push(r.measurement + ': could not read a post-restore count (verify query itself may need checking).');\n    } else if (r.post_restore_count === 0) {\n        warnings.push(r.measurement + ': 0 points found after restore — likely means this measurement\\'s write failed or its CSV was empty.');\n    }\n});\n\nmsg.verify_results = results;\nmsg.verify_warnings = warnings;\nmsg.secrets = flow.get('wdr_secrets');\nmsg.job_started_at = flow.get('wdr_job_started_at');\nmsg.total_lines_restored = flow.get('wdr_total_lines_restored') || 0;\nif (warnings.length) {\n    node.warn('Restore verify pass found ' + warnings.length + ' issue(s): ' + warnings.join(' | '));\n}\nreturn msg;",
    "outputs": 1,
    "x": 480,
    "y": 420,
    "wires": [ [ "n_wdr_job_success" ] ]
  },
  {
    "id": "n_wdr_job_success",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Log success + update HA status",
    "func": "const nowNs = Date.now() * 1000000;\nconst durationMs = Date.now() - new Date(msg.job_started_at || Date.now()).getTime();\nconst warnCount = (msg.verify_warnings || []).length;\nmsg.influx_finish_point = 'sync_job_runs,job_name=wherewhen_restore,status=success total_lines_restored=' + (msg.total_lines_restored || 0) + ',verify_warning_count=' + warnCount + ',duration_ms=' + durationMs + ' ' + nowNs;\nmsg.ha_entities = [\n    { entity_id: 'input_datetime.lucius_wherewhen_restore_last_success', value: new Date().toISOString() },\n    { entity_id: 'input_boolean.lucius_wherewhen_restore_last_attempt_ok', value: warnCount ? 'off' : 'on' },\n    { entity_id: 'input_datetime.lucius_wherewhen_restore_last_attempt', value: new Date().toISOString() }\n];\nif (!msg.secrets) msg.secrets = flow.get('wdr_secrets');\nreturn msg;",
    "outputs": 1,
    "x": 730,
    "y": 480,
    "wires": [ [ "n_wdr_final_influx_write", "n_wdr_set_ha_entities" ] ]
  },
  {
    "id": "n_wdr_final_influx_write",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Prepare finish-point write",
    "func": "msg.url = msg.secrets.influxdb_url + '/api/v2/write?org=' + msg.secrets.influxdb_org + '&bucket=' + msg.secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + msg.secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = msg.influx_finish_point;\nreturn msg;",
    "outputs": 1,
    "x": 990,
    "y": 480,
    "wires": [ [ "n_wdr_final_influx_http" ] ]
  },
  {
    "id": "n_wdr_final_influx_http",
    "type": "http request",
    "z": "tab_wherewhen_restore",
    "name": "InfluxDB: write finish point",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 220,
    "y": 540,
    "wires": [ [] ]
  },
  {
    "id": "n_wdr_set_ha_entities",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Split entities for HA WebSocket node",
    "func": "function buildServiceCall(entity_id, value) {\n    const domain = entity_id.split('.')[0];\n    if (domain === 'input_boolean') {\n        return { domain, service: value === 'on' ? 'turn_on' : 'turn_off', target: { entity_id }, data: {} };\n    }\n    if (domain === 'input_text') {\n        return { domain, service: 'set_value', target: { entity_id }, data: { value: String(value) } };\n    }\n    if (domain === 'input_datetime') {\n        return { domain, service: 'set_datetime', target: { entity_id }, data: { datetime: value } };\n    }\n    node.warn('No known service mapping for entity domain: ' + domain);\n    return null;\n}\nreturn (msg.ha_entities || []).map(e => {\n    const call = buildServiceCall(e.entity_id, e.value);\n    if (!call) return null;\n    return { payload: { action: call.domain + '.' + call.service, target: call.target, data: call.data } };\n}).filter(Boolean);",
    "outputs": 1,
    "x": 480,
    "y": 540,
    "wires": [ [ "n_wdr_ha_set_state" ] ]
  },
  {
    "id": "n_wdr_ha_set_state",
    "type": "api-call-service",
    "z": "tab_wherewhen_restore",
    "name": "Call HA service (set helper entity)",
    "server": "",
    "version": 5,
    "debugenabled": false,
    "domain": "",
    "service": "",
    "areaId": [],
    "deviceId": [],
    "entityId": [],
    "data": "",
    "dataType": "jsonata",
    "mergeContext": "",
    "mustacheAltTags": false,
    "outputProperties": [],
    "queue": "none",
    "domainType": "msg.domain",
    "serviceType": "msg.service",
    "targetType": "msg.target",
    "dataFieldType": "msg.payload",
    "comment": "Same node config as every other flow's equivalent — msg.payload carries {action, target, data} directly. Must set the 'Server' field manually per install.",
    "x": 220,
    "y": 600,
    "wires": [ [] ]
  },
  {
    "id": "n_wdr_catch",
    "type": "catch",
    "z": "tab_wherewhen_restore",
    "name": "Catch any flow error (job_failed gap, Aug 2026)",
    "scope": null,
    "uncaught": false,
    "comment": "Fixes the gap flagged in PLAN.md §20's 'Known gap' note — this flow previously only handled its success path. Nothing in this flow has a native error output, so a catch node is what actually detects a real failure (a missing/corrupt manifest, a bad CSV parse, an InfluxDB write error). Scope null = catches any node on this tab. Given this is the highest-stakes flow in the project (§20's own framing), a caught failure here matters more than anywhere else — it means a restore didn't fully succeed and needs a human look, not silent trust.",
    "x": 300,
    "y": 680,
    "wires": [ [ "n_wdr_job_failed" ] ]
  },
  {
    "id": "n_wdr_job_failed",
    "type": "function",
    "z": "tab_wherewhen_restore",
    "name": "Log failure + update HA status",
    "func": "const nowNs = Date.now() * 1000000;\nconst detail = ((msg.error && msg.error.message) || 'unknown error').toString();\nconst source = (msg.error && msg.error.source && msg.error.source.name) || 'unknown node';\nconst safeDetail = (source + ': ' + detail).replace(/([ ,=\"])/g, '\\\\$1');\nmsg.secrets = flow.get('wdr_secrets');\nmsg.job_started_at = flow.get('wdr_job_started_at');\nmsg.influx_finish_point = 'sync_job_runs,job_name=wherewhen_restore,status=failed,failure_code=flow_error detail=\"' + safeDetail + '\" ' + nowNs;\nmsg.ha_entities = [\n    { entity_id: 'input_boolean.lucius_wherewhen_restore_last_attempt_ok', value: 'off' },\n    { entity_id: 'input_datetime.lucius_wherewhen_restore_last_attempt', value: new Date().toISOString() },\n    { entity_id: 'input_text.lucius_wherewhen_restore_last_error', value: (source + ': ' + detail).slice(0, 250) }\n];\nnode.warn('wherewhen Data Restore failed: ' + source + ': ' + detail);\nreturn msg;",
    "outputs": 1,
    "x": 560,
    "y": 680,
    "wires": [ [ "n_wdr_final_influx_write", "n_wdr_set_ha_entities" ] ]
  }
]
