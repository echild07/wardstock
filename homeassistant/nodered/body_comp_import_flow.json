[
  {
    "id": "tab_bodycomp_import",
    "type": "tab",
    "label": "Lucius - Body Composition Import",
    "disabled": false,
    "info": "Runs daily around noon (PLAN.md §14). Watches /share/lucius_body_comp_import/ for .xlsx exports from Ward's smart scale, parses every file with node-red-contrib-officedocs (API confirmed working via body_comp_xlsx_test.json — see that flow's tab info for how the exceljs 'read'/'readRange' operations were verified), collapses to one reading per calendar day (keeping the latest), writes all 13 rich metrics to InfluxDB's body_composition measurement, pushes just that day's weight to GoDaddy's api/weight_push.php (which only ever sets daily_logs.weight if it's currently NULL — never overwrites), then moves processed files into processed/.\n\nFirst run backfills automatically — nothing future-only about this design: every reading in every file present gets processed every run, so a first-ever run against a file with months of history pushes all of it, exactly as PLAN.md §14 asks for.\n\nUNTESTED — same caveat as the other two live flows, plus one this flow adds on top: the exact row count of a real export is unknown ahead of time, and officedocs' readRange requires an explicit range string (confirmed via body_comp_xlsx_test.json, e.g. 'A1:Q41' for a 41-row sample). This flow requests a generously oversized range ('A1:Q5000') and filters out trailing empty rows itself, rather than assuming a fixed row count — not verified against a real oversized-range read, worth confirming on first real test whether officedocs errors or just returns blank cells past the actual data. Column order (A–Q, 17 columns) is taken directly from the confirmed table in PLAN.md §14, matched against the same 'A1:Q41' range already proven correct in the test flow."
  },
  {
    "id": "n_bci_trigger",
    "type": "inject",
    "z": "tab_bodycomp_import",
    "name": "Daily ~noon",
    "props": [
      {
        "p": "payload"
      }
    ],
    "repeat": "",
    "crontab": "0 12 * * *",
    "once": false,
    "onceDelay": 0.1,
    "topic": "",
    "payload": "",
    "payloadType": "date",
    "x": 140,
    "y": 60,
    "wires": [
      [
        "n_bci_read_secrets_file"
      ]
    ]
  },
  {
    "id": "n_bci_read_secrets_file",
    "type": "file in",
    "z": "tab_bodycomp_import",
    "name": "Read /share/lucius_secrets.json",
    "filename": "/share/lucius_secrets.json",
    "filenameType": "str",
    "format": "utf8",
    "chunk": false,
    "sendError": false,
    "encoding": "none",
    "allProps": false,
    "x": 400,
    "y": 60,
    "wires": [
      [
        "n_bci_load_secrets"
      ]
    ]
  },
  {
    "id": "n_bci_load_secrets",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Load secrets + start job log",
    "func": "// No require('fs') — same fix as the other two flows, see their\n// equivalent nodes for the full explanation (Function nodes are\n// sandboxed and have never exposed require()).\nlet secrets;\ntry {\n    secrets = JSON.parse(msg.payload);\n} catch (e) {\n    node.error('Could not parse secrets file: ' + e.message, msg);\n    return null;\n}\nmsg.secrets = secrets;\nmsg.job_started_at = new Date().toISOString();\nconst nowNs = Date.now() * 1000000;\nmsg.url = secrets.influxdb_url + '/api/v2/write?org=' + secrets.influxdb_org + '&bucket=' + secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = 'sync_job_runs,job_name=bodycomp_import,status=started value=1 ' + nowNs;\nreturn msg;",
    "outputs": 1,
    "x": 660,
    "y": 60,
    "wires": [
      [
        "n_bci_write_start_http"
      ]
    ]
  },
  {
    "id": "n_bci_write_start_http",
    "type": "http request",
    "z": "tab_bodycomp_import",
    "name": "InfluxDB: write start point",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 920,
    "y": 60,
    "wires": [
      [
        "n_bci_list_files"
      ]
    ]
  },
  {
    "id": "n_bci_list_files",
    "type": "exec",
    "z": "tab_bodycomp_import",
    "command": "ls -1 /share/lucius_body_comp_import/*.xlsx",
    "addpay": false,
    "append": "",
    "useSpawn": "false",
    "timer": "10",
    "winHide": false,
    "oldrc": false,
    "name": "List *.xlsx in watched folder",
    "x": 200,
    "y": 140,
    "wires": [
      [
        "n_bci_parse_file_list"
      ],
      [],
      []
    ]
  },
  {
    "id": "n_bci_parse_file_list",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Parse file list (empty = nothing to do)",
    "func": "// `ls` with no matches writes to stderr and exits non-zero — stdout\n// (this node's output 1, wired here) is simply empty in that case,\n// which is the normal/expected \"nothing new to import\" outcome, not\n// a failure.\nconst files = (msg.payload || '').split('\\n').map(s => s.trim()).filter(Boolean);\nif (!files.length) {\n    return [null, msg];\n}\nmsg.files = files;\nmsg.file_index = 0;\nmsg.all_rows = [];\nreturn [msg, null];",
    "outputs": 2,
    "x": 460,
    "y": 140,
    "wires": [
      [
        "n_bci_next_file"
      ],
      [
        "n_bci_no_files_success"
      ]
    ]
  },
  {
    "id": "n_bci_next_file",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Next file, or done reading",
    "func": "if (msg.file_index >= msg.files.length) {\n    // All files read into msg.all_rows — move on to the merge rules.\n    return [null, msg];\n}\nmsg.params = { source: msg.files[msg.file_index] };\ndelete msg._doc; // never carry a stale workbook object into a fresh read\nreturn [msg, null];",
    "outputs": 2,
    "x": 700,
    "y": 140,
    "wires": [
      [
        "n_bci_exceljs_read"
      ],
      [
        "n_bci_collapse_rows"
      ]
    ]
  },
  {
    "id": "n_bci_exceljs_read",
    "type": "exceljs",
    "z": "tab_bodycomp_import",
    "name": "exceljs: read",
    "operation": "read",
    "comment": "Confirmed working field name (params.source) via body_comp_xlsx_test.json.",
    "x": 940,
    "y": 140,
    "wires": [
      [
        "n_bci_set_range"
      ],
      [
        "n_bci_job_failed"
      ]
    ]
  },
  {
    "id": "n_bci_set_range",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Set oversized range (row count unknown ahead of time)",
    "func": "// A1:Q covers the confirmed 17-column layout (PLAN.md §14's table,\n// same column order already proven correct for the 'A1:Q41' test\n// range). Row count isn't known ahead of time for a real export, so\n// this deliberately over-requests (5000 rows) and the next function\n// filters out anything past the real data — UNVERIFIED against a real\n// oversized readRange call, see this tab's info note.\nmsg.params = { range: 'A1:Q5000', sheet: 'Sheet1' };\nreturn msg;",
    "outputs": 1,
    "x": 700,
    "y": 200,
    "wires": [
      [
        "n_bci_exceljs_readrange"
      ]
    ]
  },
  {
    "id": "n_bci_exceljs_readrange",
    "type": "exceljs",
    "z": "tab_bodycomp_import",
    "name": "exceljs: readRange",
    "operation": "readRange",
    "comment": "Field is 'sheet', not 'sheetName' — corrected in body_comp_xlsx_test.json.",
    "x": 940,
    "y": 200,
    "wires": [
      [
        "n_bci_extract_rows"
      ],
      [
        "n_bci_job_failed"
      ]
    ]
  },
  {
    "id": "n_bci_extract_rows",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Extract + type-convert this file's rows",
    "func": "// Output shape confirmed via body_comp_xlsx_test.json: array of arrays,\n// row 0 is headers, every value is a string regardless of column type\n// (explicit parseFloat/parseInt needed — nothing arrives pre-typed).\n// Column order (A-Q, confirmed from PLAN.md §14's table against the\n// same range already proven correct): 0 Measure Time, 1 Weight(lb),\n// 2 Body Fat%, 3 BMI, 4 Skeletal Muscle%, 5 Muscle Mass(lb),\n// 6 Protein%, 7 BMR(kcal), 8 Fat-free Body Weight(lb),\n// 9 Subcutaneous Fat%, 10 Visceral Fat, 11 Body Water%, 12 Bone Mass(lb),\n// 13 Metabolic Age, 14 Body Type (DROPPED — Ward's call, \"their\n// analytics, not what we will build\"), 15 Device MAC, 16 Device Name.\nconst rows = Array.isArray(msg.payload) ? msg.payload : [];\nconst dataRows = rows.slice(1); // skip header row\nconst num = v => { const n = parseFloat(v); return Number.isFinite(n) ? n : null; };\n\ndataRows.forEach(r => {\n    if (!r || !r[0]) return; // trailing empty row from the oversized range request\n    const measureTime = String(r[0]).trim();\n    const ts = new Date(measureTime);\n    if (isNaN(ts.getTime())) return; // not a real data row\n    msg.all_rows.push({\n        measure_time: measureTime,\n        ts_ms: ts.getTime(),\n        date: ts.toISOString().slice(0, 10),\n        weight_lb: num(r[1]),\n        body_fat_pct: num(r[2]),\n        bmi: num(r[3]),\n        skeletal_muscle_pct: num(r[4]),\n        muscle_mass_lb: num(r[5]),\n        protein_pct: num(r[6]),\n        bmr_kcal: num(r[7]),\n        fat_free_body_weight_lb: num(r[8]),\n        subcutaneous_fat_pct: num(r[9]),\n        visceral_fat: num(r[10]),\n        body_water_pct: num(r[11]),\n        bone_mass_lb: num(r[12]),\n        metabolic_age: num(r[13]),\n        device_mac: r[15] ? String(r[15]).trim() : 'unknown',\n        device_name: r[16] ? String(r[16]).trim() : 'unknown',\n    });\n});\n\nmsg.file_index += 1;\ndelete msg._doc; // avoid carrying the workbook object (circular refs) any further than needed\nreturn msg;",
    "outputs": 1,
    "x": 1180,
    "y": 200,
    "wires": [
      [
        "n_bci_next_file"
      ]
    ]
  },
  {
    "id": "n_bci_collapse_rows",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Merge rule 1+2: collapse to latest-per-day",
    "func": "// Rule 1 (PLAN.md §14): collapse to one reading per calendar day,\n// keeping the latest-timestamped row — applies across ALL files read\n// this run, not per-file, since a re-export can overlap a previous one.\nconst byDate = {};\n(msg.all_rows || []).forEach(row => {\n    const existing = byDate[row.date];\n    if (!existing || row.ts_ms > existing.ts_ms) byDate[row.date] = row;\n});\nconst chosen = Object.values(byDate);\n\nfunction esc(s) { return String(s).replace(/([ ,=])/g, '\\\\$1'); }\nconst lines = [];\nconst weightPushes = [];\nchosen.forEach(r => {\n    const tsNs = r.ts_ms * 1000000;\n    const fields = [];\n    ['weight_lb','body_fat_pct','bmi','skeletal_muscle_pct','muscle_mass_lb',\n     'protein_pct','bmr_kcal','fat_free_body_weight_lb','subcutaneous_fat_pct',\n     'visceral_fat','body_water_pct','bone_mass_lb','metabolic_age'].forEach(f => {\n        if (r[f] !== null && r[f] !== undefined) fields.push(f + '=' + r[f]);\n    });\n    if (fields.length) {\n        lines.push('body_composition,device_mac=' + esc(r.device_mac) + ',device_name=' + esc(r.device_name) + ' ' + fields.join(',') + ' ' + tsNs);\n    }\n    // Rule 2 (weight_push.php's own job, not duplicated here): the\n    // never-overwrite check happens server-side — this flow just sends\n    // every collapsed day's weight and lets GoDaddy decide whether it's\n    // a no-op.\n    if (r.weight_lb !== null && r.weight_lb !== undefined) {\n        weightPushes.push({ date: r.date, weight_lb: r.weight_lb });\n    }\n});\n\nmsg.influx_lines = lines;\nmsg.weight_pushes = weightPushes;\nmsg.record_count = chosen.length;\n\n// Split/join (for the per-day weight-push loop below) doesn't reliably\n// carry arbitrary top-level msg properties through every message in the\n// group — stash what the tail of the flow needs in flow context instead\n// of trusting that propagation.\nflow.set('bci_secrets', msg.secrets);\nflow.set('bci_job_started_at', msg.job_started_at);\nreturn msg;",
    "outputs": 1,
    "x": 940,
    "y": 260,
    "wires": [
      [
        "n_bci_write_influx_batch"
      ]
    ]
  },
  {
    "id": "n_bci_write_influx_batch",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Prepare InfluxDB batch write",
    "func": "if (!msg.influx_lines.length) {\n    return [null, msg];\n}\nmsg.url = msg.secrets.influxdb_url + '/api/v2/write?org=' + msg.secrets.influxdb_org + '&bucket=' + msg.secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + msg.secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = msg.influx_lines.join('\\n');\nreturn [msg, msg];",
    "outputs": 2,
    "x": 1180,
    "y": 260,
    "wires": [
      [
        "n_bci_influx_write_http"
      ],
      [
        "n_bci_start_weight_pushes"
      ]
    ]
  },
  {
    "id": "n_bci_influx_write_http",
    "type": "http request",
    "z": "tab_bodycomp_import",
    "name": "InfluxDB: write body_composition batch",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 1440,
    "y": 240,
    "wires": [
      []
    ]
  },
  {
    "id": "n_bci_start_weight_pushes",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Prep weight_pushes array for split",
    "func": "msg.payload = msg.weight_pushes || [];\nreturn msg;",
    "outputs": 1,
    "x": 1180,
    "y": 320,
    "wires": [
      [
        "n_bci_split"
      ]
    ]
  },
  {
    "id": "n_bci_split",
    "type": "split",
    "z": "tab_bodycomp_import",
    "name": "Split (one weight push per day)",
    "splt": "\\n",
    "spltType": "str",
    "arraySplt": 1,
    "arraySpltType": "len",
    "stream": false,
    "addname": "",
    "x": 200,
    "y": 380,
    "wires": [
      [
        "n_bci_prep_weight_push"
      ]
    ]
  },
  {
    "id": "n_bci_prep_weight_push",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Prepare weight_push.php request",
    "func": "const secrets = flow.get('bci_secrets');\nconst day = msg.payload; // { date, weight_lb }\nmsg.url = secrets.godaddy_base_url + '/api/weight_push.php';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Bearer ' + secrets.godaddy_api_sync_token, 'Content-Type': 'application/json' };\nmsg.payload = JSON.stringify({ date: day.date, weight_lb: day.weight_lb });\nreturn msg;",
    "outputs": 1,
    "x": 460,
    "y": 380,
    "wires": [
      [
        "n_bci_weight_push_http"
      ]
    ]
  },
  {
    "id": "n_bci_weight_push_http",
    "type": "http request",
    "z": "tab_bodycomp_import",
    "name": "GoDaddy: POST weight_push.php",
    "method": "use",
    "ret": "obj",
    "url": "",
    "x": 720,
    "y": 380,
    "wires": [
      [
        "n_bci_join"
      ]
    ]
  },
  {
    "id": "n_bci_join",
    "type": "join",
    "z": "tab_bodycomp_import",
    "name": "Join (wait for all weight pushes)",
    "mode": "auto",
    "build": "array",
    "property": "payload",
    "propertyType": "msg",
    "key": "topic",
    "joiner": "\\n",
    "joinerType": "str",
    "accumulate": false,
    "timeout": "",
    "count": "",
    "reduceRight": false,
    "x": 960,
    "y": 380,
    "wires": [
      [
        "n_bci_after_weight_pushes"
      ]
    ]
  },
  {
    "id": "n_bci_after_weight_pushes",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Tally results, prep processed/ move",
    "func": "const results = Array.isArray(msg.payload) ? msg.payload : [];\nconst written = results.filter(r => r && r.success && r.weight_written).length;\nmsg.weight_pushes_written = written;\nmsg.weight_pushes_total = results.length;\nmsg.job_started_at = flow.get('bci_job_started_at');\nmsg.secrets = flow.get('bci_secrets');\n\nconst files = msg.files || [];\nif (!files.length) {\n    return [null, msg];\n}\nconst quoted = files.map(f => \"'\" + f.replace(/'/g, \"'\\\\''\") + \"'\").join(' ');\nmsg.payload = 'mkdir -p /share/lucius_body_comp_import/processed && mv ' + quoted + ' /share/lucius_body_comp_import/processed/';\nreturn [msg, null];",
    "outputs": 2,
    "x": 200,
    "y": 440,
    "wires": [
      [
        "n_bci_move_processed"
      ],
      [
        "n_bci_job_success"
      ]
    ]
  },
  {
    "id": "n_bci_move_processed",
    "type": "exec",
    "z": "tab_bodycomp_import",
    "command": "",
    "addpay": "payload",
    "append": "",
    "useSpawn": "false",
    "timer": "10",
    "winHide": false,
    "oldrc": false,
    "name": "Move processed files (msg.payload = full shell command)",
    "x": 460,
    "y": 440,
    "wires": [
      [
        "n_bci_job_success"
      ],
      [
        "n_bci_job_success"
      ],
      []
    ]
  },
  {
    "id": "n_bci_no_files_success",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Nothing to import this run",
    "func": "msg.record_count = 0;\nmsg.weight_pushes_written = 0;\nmsg.weight_pushes_total = 0;\nreturn msg;",
    "outputs": 1,
    "x": 700,
    "y": 100,
    "wires": [
      [
        "n_bci_job_success"
      ]
    ]
  },
  {
    "id": "n_bci_job_success",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Log success + update HA status",
    "func": "const nowNs = Date.now() * 1000000;\nconst durationMs = Date.now() - new Date(msg.job_started_at || Date.now()).getTime();\nmsg.influx_finish_point = 'sync_job_runs,job_name=bodycomp_import,status=success record_count=' + (msg.record_count||0) + ',weight_pushes_written=' + (msg.weight_pushes_written||0) + ',duration_ms=' + durationMs + ' ' + nowNs;\nmsg.sqlite_job = { job_name: 'bodycomp_import', mode: 'success', last_success: new Date().toISOString(), last_attempt: new Date().toISOString(), last_attempt_ok: 1 };\nif (!msg.secrets) msg.secrets = flow.get('bci_secrets');\nreturn msg;",
    "outputs": 1,
    "x": 700,
    "y": 500,
    "wires": [
      [
        "n_bci_final_influx_write",
        "n_bci_sqlite_ensure_table"
      ]
    ]
  },
  {
    "id": "n_bci_job_failed",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Log failure + update HA status",
    "func": "const nowNs = Date.now() * 1000000;\nconst detail = (node.error ? 'exceljs read/readRange failed' : 'unknown').replace(/([ ,=\"])/g, '\\\\$1');\nmsg.secrets = flow.get('bci_secrets');\nmsg.job_started_at = flow.get('bci_job_started_at');\nmsg.influx_finish_point = 'sync_job_runs,job_name=bodycomp_import,status=failed,failure_code=xlsx_parse_error detail=\"' + detail + '\" ' + nowNs;\nmsg.sqlite_job = { job_name: 'bodycomp_import', mode: 'failure', last_attempt: new Date().toISOString(), last_attempt_ok: 0, last_error: 'xlsx_parse_error: could not read/parse an .xlsx file in the watched folder' };\nnode.warn('Body Composition Import failed: could not read/parse an xlsx file');\nreturn msg;",
    "outputs": 1,
    "x": 1180,
    "y": 500,
    "wires": [
      [
        "n_bci_final_influx_write",
        "n_bci_sqlite_ensure_table"
      ]
    ]
  },
  {
    "id": "n_bci_final_influx_write",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "Prepare finish-point write",
    "func": "msg.url = msg.secrets.influxdb_url + '/api/v2/write?org=' + msg.secrets.influxdb_org + '&bucket=' + msg.secrets.influxdb_bucket + '&precision=ns';\nmsg.method = 'POST';\nmsg.headers = { 'Authorization': 'Token ' + msg.secrets.influxdb_token, 'Content-Type': 'text/plain; charset=utf-8' };\nmsg.payload = msg.influx_finish_point;\nreturn msg;",
    "outputs": 1,
    "x": 700,
    "y": 560,
    "wires": [
      [
        "n_bci_final_influx_http"
      ]
    ]
  },
  {
    "id": "n_bci_final_influx_http",
    "type": "http request",
    "z": "tab_bodycomp_import",
    "name": "InfluxDB: write finish point",
    "method": "use",
    "ret": "txt",
    "url": "",
    "x": 940,
    "y": 560,
    "wires": [
      []
    ]
  },
  {
    "id": "n_bci_sqlite_ensure_table",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "SQLite: ensure job_status table exists",
    "func": "// Ensures the shared status table exists before every write — cheap\n// no-op if it already does. Same idempotent CREATE TABLE IF NOT EXISTS\n// discipline as godaddy/sql/upgrade_from_3.0.0.sql, just run inline\n// rather than as a separate manual step (Ward, Aug 2026 — \"a node red\n// task that initializes it, and keeps it up to date\"). Identical in\n// every flow that writes status — duplicated on purpose, matching this\n// project's existing preference for self-contained nodes over a shared-\n// context module pattern.\nmsg.topic = \"CREATE TABLE IF NOT EXISTS job_status (\" +\n    \"job_name TEXT PRIMARY KEY, last_success TEXT, last_attempt TEXT, \" +\n    \"last_attempt_ok INTEGER, last_error TEXT, checkpoint TEXT, updated_at TEXT)\";\nreturn msg;",
    "outputs": 1,
    "x": 460,
    "y": 620,
    "wires": [
      [
        "n_bci_sqlite_ensure_table_db"
      ]
    ]
  },
  {
    "id": "n_bci_sqlite_ensure_table_db",
    "type": "sqlite",
    "z": "tab_bodycomp_import",
    "name": "sqlite: CREATE TABLE IF NOT EXISTS",
    "mydb": "cfg_lucius_status_db",
    "sqlquery": "msg.topic",
    "sql": "",
    "x": 700,
    "y": 620,
    "wires": [
      [
        "n_bci_sqlite_upsert_prep"
      ]
    ]
  },
  {
    "id": "n_bci_sqlite_upsert_prep",
    "type": "function",
    "z": "tab_bodycomp_import",
    "name": "SQLite: prep status UPSERT",
    "func": "// Builds the UPSERT from msg.sqlite_job (set by job_success/job_failed\n// above). 'success' mode updates last_success and clears last_error;\n// 'failure' mode updates last_error and deliberately leaves last_success\n// untouched (a failure shouldn't erase the last known-good run). checkpoint\n// is job-specific optional state (only oura_sync uses it, for its self-heal\n// fetch window) — only touched when the caller actually supplies one.\nfunction esc(s) { return \"'\" + String(s).replace(/'/g, \"''\") + \"'\"; }\nfunction sqlVal(v) { return (v === null || v === undefined) ? 'NULL' : (typeof v === 'number' ? v : esc(v)); }\nconst j = msg.sqlite_job;\nconst now = new Date().toISOString();\nconst cols = ['job_name', 'last_success', 'last_attempt', 'last_attempt_ok', 'last_error', 'checkpoint', 'updated_at'];\nconst vals = [j.job_name, j.last_success ?? null, j.last_attempt, j.last_attempt_ok, j.last_error ?? null, j.checkpoint ?? null, now];\nconst setClauses = ['last_attempt=excluded.last_attempt', 'last_attempt_ok=excluded.last_attempt_ok', 'updated_at=excluded.updated_at'];\nif (j.mode === 'success') {\n    setClauses.push('last_success=excluded.last_success', 'last_error=NULL');\n    if (j.checkpoint !== undefined && j.checkpoint !== null) setClauses.push('checkpoint=excluded.checkpoint');\n} else {\n    setClauses.push('last_error=excluded.last_error');\n}\nmsg.topic = 'INSERT INTO job_status (' + cols.join(', ') + ') VALUES (' + vals.map(sqlVal).join(', ') + ') ' +\n    'ON CONFLICT(job_name) DO UPDATE SET ' + setClauses.join(', ');\nreturn msg;",
    "outputs": 1,
    "x": 460,
    "y": 680,
    "wires": [
      [
        "n_bci_sqlite_upsert_db"
      ]
    ]
  },
  {
    "id": "n_bci_sqlite_upsert_db",
    "type": "sqlite",
    "z": "tab_bodycomp_import",
    "name": "sqlite: UPSERT job_status",
    "mydb": "cfg_lucius_status_db",
    "sqlquery": "msg.topic",
    "sql": "",
    "x": 700,
    "y": 680,
    "wires": [
      []
    ]
  },
  {
    "id": "cfg_lucius_status_db",
    "type": "sqlitedb",
    "z": "",
    "name": "Lucius status DB",
    "db": "/share/lucius_status.db",
    "mode": "RWC"
  }
]
